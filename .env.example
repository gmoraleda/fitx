# Example environment file (no real secrets here)

# FitX course configuration (not secret)
FITX_COURSE_ID=53

# HTTP server port (host side). Container always listens on 8787 (not secret)
PORT=8787

# Refresh interval in seconds (15 minutes default) (not secret)
REFRESH_INTERVAL_SECONDS=900

# Optional cookie for FitX (SECRET). Leave empty here.
# FITX_COOKIE=

# Optional refresh token to protect POST /refresh (SECRET). Leave empty here.
# REFRESH_TOKEN=

# Timezone used inside the container (not secret)
TZ=Europe/Berlin

# Cloudflare Tunnel token when using Docker-based tunnel (SECRET). Leave empty here.
# TUNNEL_TOKEN=

# Comma-separated keywords to exclude by title (case-insensitive)
# Default excludes common FitX classes you may not want in your feed.
# Examples: booty x, xamba, x step, fatburn x
FITX_EXCLUDE_KEYWORDS=booty x,xamba,x step,fatburn x,into x,cardiobox,sixpack,functional x

# Cloudflare Tunnel token (from Cloudflare dashboard)
# Used by the cloudflared sidecar to expose the service publicly.
# Treat as secret; do not commit a real token.
# CLOUDFLARED_TOKEN=eyJ...
